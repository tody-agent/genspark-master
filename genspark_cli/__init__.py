"""Genspark CLI — Multi-Account AI Gateway.

Browser-session client for Genspark chat and image generation: a Python
CLI (`genspark` / `gsk`), an MCP server (`genspark-mcp`), and a local
OpenAI-compatible proxy. Authentication uses browser cookies and a
reCAPTCHA token captured from a headed Genspark session — no API key.
"""

__version__ = "0.5.0"
